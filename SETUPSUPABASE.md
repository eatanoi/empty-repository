# 🗄️ คู่มือติดตั้งระบบชั้นเรียนด้วย Supabase

ระบบนี้ให้ **ฐานข้อมูลจริง (PostgreSQL) + แดชบอร์ดครูแบบอัปเดตสด** รองรับหลายห้อง หลายภาคเรียน
ใช้แพ็กเกจฟรีของ Supabase ได้สบายสำหรับโรงเรียน (ฟรีให้ฐานข้อมูล 500 MB — คะแนน 1 แถวใช้พื้นที่ไม่ถึง 1 KB
คิดคร่าว ๆ คือเก็บได้หลายแสนแถว)

**สิ่งที่จะได้**
- นักเรียนสแกน QR → กรอกชื่อ → เล่น → คะแนนเข้าฐานข้อมูลทันที (ไม่มีเน็ตก็เล่นได้ ค่อยส่งทีหลัง)
- ครูเปิด `dashboard.html` เห็นความก้าวหน้าแบบเรียลไทม์ แยกตามห้อง/ภาคเรียน ดาวน์โหลด CSV ได้
- เปิด–ปิดห้องได้ ปิดแล้วฐานข้อมูลจะไม่รับคะแนนใหม่จากห้องนั้น

---

## ขั้นที่ 1 · สร้างโปรเจกต์ Supabase (5 นาที)

1. ไปที่ [supabase.com](https://supabase.com) → **Start your project** → สมัคร/เข้าสู่ระบบด้วย GitHub หรืออีเมล
2. **New project**
   - Name: `genetics-quest` (ตั้งชื่ออะไรก็ได้)
   - Database Password: ตั้งรหัสแล้ว**เก็บไว้** (ใช้เฉพาะเวลาต่อฐานข้อมูลตรง ๆ ไม่ได้ใช้ในเกม)
   - Region: **Southeast Asia (Singapore)** — ใกล้ไทยที่สุด
3. รอสร้างเสร็จประมาณ 2 นาที

## ขั้นที่ 2 · สร้างตาราง (1 นาที)

1. เมนูซ้าย → **SQL Editor** → **New query**
2. วางเนื้อหาทั้งหมดจากไฟล์ [`supabase/schema.sql`](supabase/schema.sql) → กด **Run**
3. ต้องขึ้น `Success. No rows returned` — ถ้ามี error ให้รันซ้ำได้ ไฟล์นี้เขียนให้รันกี่รอบก็ปลอดภัย

สิ่งที่สคริปต์นี้ทำให้:

| สิ่งที่สร้าง | หน้าที่ |
|---|---|
| ตาราง `classes` | เก็บห้องเรียน (รหัสห้อง ชื่อ ภาคเรียน เปิด/ปิด) |
| ตาราง `scores` | เก็บผลการเล่นทีละด่าน |
| Row Level Security | **นักเรียนเพิ่มคะแนนได้อย่างเดียว อ่านข้อมูลใครไม่ได้เลย** · ครูอ่าน/ลบได้เฉพาะห้องของตัวเอง |
| ฟังก์ชัน `room_is_open()` | ตรวจว่าห้องนั้นมีอยู่จริงและยังเปิดรับคะแนน |
| Realtime | เปิดให้แดชบอร์ดรับข้อมูลใหม่แบบสด |

## ขั้นที่ 3 · สร้างบัญชีครู (2 นาที)

1. เมนูซ้าย → **Authentication** → **Users** → **Add user** → *Create new user*
2. ใส่อีเมลกับรหัสผ่านที่จะใช้เข้าแดชบอร์ด → ติ๊ก **Auto Confirm User** (สำคัญ ไม่งั้นต้องยืนยันอีเมลก่อน)
3. ถ้าต้องการหลายบัญชีครู เพิ่มได้เรื่อย ๆ — แต่ละคนจะเห็นเฉพาะห้องที่ตัวเองสร้าง

## ขั้นที่ 4 · คัดลอกกุญแจ 2 ตัว

เมนูซ้าย → **Project Settings** → **Data API** (บางเวอร์ชันอยู่ที่ API)

| ต้องใช้ | หน้าตา | เอาไปทำอะไร |
|---|---|---|
| **Project URL** | `https://abcdefgh.supabase.co` | ทั้งเกมและแดชบอร์ด |
| **anon public key** | `eyJhbGciOi...` หรือ `sb_publishable_...` | ทั้งเกมและแดชบอร์ด |

> ⚠️ **ห้ามใช้ `service_role` key เด็ดขาด** คีย์นั้นข้าม RLS ได้ทั้งหมด ถ้าหลุดไปอยู่ในหน้าเว็บ
> ใครก็แก้/ลบข้อมูลได้หมด — ใช้เฉพาะ **anon public key** ซึ่งออกแบบมาให้เปิดเผยได้อยู่แล้ว

## ขั้นที่ 5 · เปิดแดชบอร์ดและสร้างห้อง

1. อัปโหลด `dashboard.html` ขึ้น GitHub repo เดียวกับเกม (➕ → Upload files → Commit)
2. เปิด `https://eatanoi.github.io/empty-repository/dashboard.html`
3. กรอก **Project URL**, **anon key**, อีเมลและรหัสผ่านครู → **เข้าสู่ระบบ**
4. แท็บ **🏫 จัดการห้อง** → สร้างห้อง เช่น
   - รหัสห้อง `M401A` · ชื่อห้อง `ม.4/1` · ภาคเรียน `2/2569`

> รหัสห้องนี้ต้องตรงกับที่ใช้ในลิงก์ของนักเรียน และห้องต้องอยู่สถานะ **เปิดรับคะแนน**
> ไม่งั้นฐานข้อมูลจะปฏิเสธ (เป็นการกันคนสุ่มยิงข้อมูลมั่ว)

## ขั้นที่ 6 · สร้างลิงก์สำหรับนักเรียน

1. เปิดเกมพร้อมพารามิเตอร์ตั้งค่า: `https://eatanoi.github.io/empty-repository/?setup=1`
2. เลือกโหมด **🗄️ Supabase** → วาง **Project URL** และ **anon key** → ใส่รหัสห้อง `M401A`
3. กด **🔌 ทดสอบการเชื่อมต่อ** → ต้องขึ้น "✅ เชื่อมต่อสำเร็จ" และแดชบอร์ดจะเห็นแถว *"✅ ทดสอบระบบ (ครู)"* โผล่มาสด ๆ
4. กด **🔗 สร้างลิงก์สำหรับนักเรียน** → **📋 คัดลอกลิงก์** → นำไปทำ QR

ลิงก์จะหน้าตาแบบนี้ (ห้องละลิงก์)

```
https://eatanoi.github.io/empty-repository/?sb=https%3A%2F%2Fabcdefgh.supabase.co&key=eyJhbGciOi...&room=M401A
```

---

## 📊 หน้าจอในแดชบอร์ด

| แท็บ | ใช้ดูอะไร |
|---|---|
| 📈 ภาพรวมชั้นเรียน | คะแนนเฉลี่ยรายด่าน (แถบสีส้ม = ต่ำกว่า 60% ควรสอนซ้ำ) + อันดับดาวสะสม |
| 👥 รายบุคคล | ตารางไขว้ ชื่อ × ด่าน เห็นทันทีว่าใครติดด่านไหน |
| 🎯 รายด่าน | จำนวนครั้ง คะแนนเฉลี่ย ดาวเฉลี่ย ข้อที่พลาดเฉลี่ย เวลาเฉลี่ย |
| 🧾 ข้อมูลดิบ | รายการล่าสุด 300 แถว ใช้ตรวจรายคนหรือหาพฤติกรรมผิดปกติ |
| 🏫 จัดการห้อง | สร้างห้องใหม่ · เปิด/ปิดรับคะแนน |

**ป้ายมุมขวาบน** บอกสถานะการอัปเดต
- 🟢 *อัปเดตสด* = ต่อ Realtime ได้ คะแนนใหม่เด้งขึ้นทันที
- 🟠 *รีเฟรชอัตโนมัติทุก 8 วินาที* = ต่อ WebSocket ไม่ได้ (เช่นเน็ตโรงเรียนบล็อก) ระบบสลับมาโหมดดึงข้อมูลเป็นรอบให้เอง ครูยังเห็นข้อมูลครบเหมือนเดิม

---

## 🔒 ความปลอดภัยและ PDPA

- **นักเรียนอ่านข้อมูลใครไม่ได้เลย** RLS อนุญาตเฉพาะคำสั่ง `INSERT` เท่านั้น ต่อให้เอา anon key ไปยิงเองก็ดึงข้อมูลออกไม่ได้
- **ครูเห็นเฉพาะห้องของตัวเอง** ผูกกับ `teacher_id = auth.uid()`
- **ข้อมูลที่เก็บ**: รหัสห้อง ชื่อที่นักเรียนกรอกเอง รหัสอุปกรณ์แบบสุ่ม และผลการเล่น — ไม่มีอีเมล เบอร์โทร หรือตำแหน่งที่ตั้ง
- แนะนำให้แจ้งนักเรียนว่าเก็บอะไรไปทำอะไร (ในเกมมีข้อความแจ้งอยู่แล้วตอนกรอกชื่อ) และลบข้อมูลเมื่อจบภาคเรียน
- ลบข้อมูลทั้งห้องเมื่อจบเทอม: SQL Editor → `delete from public.scores where room = 'M401A';`
- **ยังกันโกงไม่ได้ 100%** คนที่รู้เทคนิคยังยิงคะแนนปลอมเข้าห้องที่เปิดอยู่ได้ (เพราะต้องเปิดให้นักเรียนส่งได้โดยไม่ต้องล็อกอิน)
  → ใช้เป็นคะแนนการมีส่วนร่วม/การบ้านฝึกฝน และดูคอลัมน์ *เวลาที่ใช้* กับ *ครั้งที่* ประกอบ
  → ถ้าจะใช้เป็นคะแนนสอบจริง ควรปิดห้องนอกเวลาเรียน เปิดเฉพาะช่วงคาบ

---

## 🔧 แก้ปัญหาที่พบบ่อย

| อาการ | วิธีแก้ |
|---|---|
| ล็อกอินไม่ผ่าน *Invalid login credentials* | ตรวจอีเมล/รหัสผ่าน และต้องติ๊ก **Auto Confirm User** ตอนสร้างผู้ใช้ |
| ล็อกอินผ่านแต่ขึ้น error เรื่องตาราง `classes` | ยังไม่ได้รัน `schema.sql` หรือรันไม่ครบ → SQL Editor รันใหม่ทั้งไฟล์ |
| เกมขึ้น *ไม่มีสิทธิ์บันทึก — ห้องอาจถูกปิดอยู่* | ยังไม่ได้สร้างห้องนั้นในแดชบอร์ด หรือห้องถูกปิด หรือรหัสห้องพิมพ์ไม่ตรง (ระบบใช้ตัวพิมพ์ใหญ่เสมอ) |
| แดชบอร์ดขึ้น *รีเฟรชอัตโนมัติทุก 8 วินาที* ตลอด | เครือข่ายบล็อก WebSocket — ใช้งานได้ตามปกติ ข้อมูลอัปเดตช้ากว่าเล็กน้อยเท่านั้น |
| คะแนนค้างในเครื่องนักเรียน | ให้นักเรียนกดป้ายมุมขวาบนในเกม → **🚀 ส่งเดี๋ยวนี้** จะบอกสาเหตุที่ส่งไม่ผ่าน |
| อยากย้ายจาก Google Sheet เดิม | ใช้พร้อมกันได้ — เกมจะใช้ปลายทางที่ตั้งค่าไว้ล่าสุดจากลิงก์ ถ้าอยากบังคับให้เป็น Supabase ให้แจกลิงก์ใหม่ให้นักเรียนสแกน |

---

## 💰 ค่าใช้จ่าย

แพ็กเกจฟรีของ Supabase (ข้อมูล ณ ปี 2026) ให้ฐานข้อมูล 500 MB, ผู้ใช้ที่ล็อกอินได้ 50,000 คน/เดือน
และแบนด์วิดท์ 5 GB/เดือน — เกินพอสำหรับการใช้ในโรงเรียน

⚠️ ข้อควรรู้: โปรเจกต์ฟรีจะถูก **พักอัตโนมัติเมื่อไม่มีการใช้งานติดต่อกันประมาณ 1 สัปดาห์**
ถ้าปิดเทอมแล้วกลับมาใช้ ให้เข้า Supabase Dashboard กด **Restore/Resume** ก่อน (ข้อมูลไม่หาย)
