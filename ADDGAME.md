# 📦 วิธีนำเกมอื่น ๆ ขึ้นเก็บไว้บน GitHub

คู่มือนี้ใช้กับรีโป `eatanoi/empty-repository` ซึ่งเปิด **GitHub Pages** อยู่แล้ว
ทุกอย่างที่อัปขึ้นรีโปนี้จะกลายเป็นเว็บที่เปิดเล่นได้ทันทีภายใน 1–2 นาที

---

## หลักคิดสั้น ๆ

> **1 เกม = 1 โฟลเดอร์ = 1 ลิงก์**
> ในโฟลเดอร์ต้องมีไฟล์ชื่อ `index.html` เสมอ

| วางไฟล์ไว้ที่ | ลิงก์ที่ได้ |
|---|---|
| `index.html` (นอกสุด) | `https://eatanoi.github.io/empty-repository/` ← หน้ารวมเกม |
| `respiration/index.html` | `https://eatanoi.github.io/empty-repository/respiration/` |
| `photosynthesis/index.html` | `https://eatanoi.github.io/empty-repository/photosynthesis/` |
| `photosynthesis/imag/bg.webp` | `.../photosynthesis/imag/bg.webp` |

ถ้าตั้งชื่อไฟล์เป็นอย่างอื่น เช่น `เกมพืช.html` ลิงก์จะยาวและพิมพ์ยาก
(`.../%E0%B9%80%E0%B8%81%E0%B8%A1...html`) จึงควรใช้ `index.html` เสมอ

### กฎตั้งชื่อโฟลเดอร์

- ใช้ **อังกฤษตัวเล็ก** เท่านั้น เช่น `photosynthesis`, `respiration`, `genetics`, `cell-division`
- **ห้าม**เว้นวรรค (ใช้ `-` แทน) · ห้ามภาษาไทย · ห้ามอักษรพิเศษ
- ตัวพิมพ์เล็ก-ใหญ่ถือว่าคนละชื่อ: `Imag/` ≠ `imag/` (เปิดในเครื่องได้ แต่บนเว็บรูปจะไม่ขึ้น)

### ชื่อโฟลเดอร์ที่แนะนำสำหรับชุดเกมชีววิทยา

| เกม | โฟลเดอร์ |
|---|---|
| ภารกิจคลอโรพลาสต์ (สังเคราะห์แสง) | `photosynthesis/` |
| โรงไฟฟ้าไมโทคอนเดรีย (การหายใจระดับเซลล์) | `respiration/` ✅ มีแล้ว |
| ภารกิจพันธุกรรม | `genetics/` (ตอนนี้อยู่นอกสุด — ดูหัวข้อ "จัดบ้านใหม่") |
| การแบ่งเซลล์ | `cell-division/` |
| ระบบต่าง ๆ ในร่างกาย | `human-body/` |

---

## ❓ ถ้าทุกเกมชื่อไฟล์ `index.html` เหมือนกันจะชนกันไหม

**ไม่ชนครับ** — ชื่อไฟล์ซ้ำกันได้ ขอแค่อยู่**คนละโฟลเดอร์** เหมือนไฟล์ในคอมพิวเตอร์
GitHub มองว่า `respiration/index.html` กับ `photosynthesis/index.html` เป็นคนละไฟล์คนละอัน

```
empty-repository/
├── index.html              ← หน้ารวมเกม (หน้าแรกของเว็บ)
├── respiration/
│   └── index.html          ← เกมการหายใจระดับเซลล์   ✔ ไม่ทับกัน
└── photosynthesis/
    └── index.html          ← เกมสังเคราะห์แสง        ✔ ไม่ทับกัน
```

> ไม่ต้องเปลี่ยนชื่อเป็น `respiration.html` หรือ `game2.html` เพราะจะทำให้ลิงก์ยาวขึ้น
> (`.../respiration.html` แทนที่จะเป็น `.../respiration/`) และเวลาเพิ่มลงหน้าจอโฮมจะแยกเกมไม่สวย

### ⚠️ จุดเดียวที่ทับกันได้จริง — ต้องระวังตรงนี้

ถ้าอยู่ที่**หน้าแรกของรีโป** (ไม่ได้เข้าไปในโฟลเดอร์ไหน) แล้วกด Upload files
แล้ว**ลากไฟล์ `index.html` เดี่ยว ๆ** เข้าไป → มันจะไป**ทับหน้ารวมเกม**ที่เป็นหน้าแรกทันที
เพราะ GitHub เห็นว่าเป็นไฟล์ชื่อเดียวกันในที่เดียวกัน

**วิธีเลี่ยง — เลือกอย่างใดอย่างหนึ่ง**

| วิธี | ทำอย่างไร |
|---|---|
| ✅ ลาก **ทั้งโฟลเดอร์** ไม่ใช่ลากไฟล์เดี่ยว | ตั้งชื่อโฟลเดอร์ในเครื่องให้ถูกก่อน เช่น `photosynthesis` (ข้างในมี `index.html`) แล้วลากทั้งโฟลเดอร์มาวาง — GitHub จะเก็บชื่อโฟลเดอร์ให้เอง |
| ✅ สร้างโฟลเดอร์ก่อนแล้วค่อยอัป | **Add file → Create new file** → พิมพ์ `photosynthesis/index.html` → วางโค้ด → Commit · หลังจากนั้นค่อย**คลิกเข้าไปในโฟลเดอร์** `photosynthesis` แล้วกด Add file → Upload files เพื่อใส่รูปเพิ่ม |

> **หัวใจคือ: ก่อนกด Upload files ให้ดูแถบ path ด้านบนว่าอยู่ในโฟลเดอร์ที่ต้องการแล้วหรือยัง**
> ถ้าเห็นแค่ `empty-repository` แปลว่าอยู่นอกสุด — ไฟล์จะลงตรงนั้น

### เผลอทับไปแล้วแก้อย่างไร

ไม่หายครับ git เก็บประวัติไว้ทุกครั้ง

1. เปิดไฟล์ `index.html` ในรีโป → กดปุ่ม **History** (มุมขวาบนของไฟล์)
2. เลือก commit ก่อนหน้าที่ยังถูกต้อง → กด **⋯ → View file** หรือ **Browse repository at this point**
3. กดดินสอ ✏️ ก๊อบปี้เนื้อหาทั้งหมด แล้วเอาไปวางทับในไฟล์ปัจจุบัน → Commit
   (หรือกด **Revert** ที่หน้า commit ที่อัปผิด ถ้ามีปุ่มให้)

---

---

## วิธีที่ 1 · อัปผ่านหน้าเว็บ GitHub (ง่ายที่สุด ไม่ต้องลงโปรแกรม)

### อัปทั้งโฟลเดอร์ทีเดียว

1. เปิด `https://github.com/eatanoi/empty-repository`
2. กด **Add file ▾ → Upload files**
3. **ลากโฟลเดอร์เกมทั้งโฟลเดอร์** จากเครื่องมาวางในกรอบ (Chrome/Edge ลากทั้งโฟลเดอร์ได้เลย
   ไฟล์ข้างในและโฟลเดอร์ย่อยอย่าง `imag/` จะติดขึ้นไปด้วยครบ)
4. ช่องล่างสุด พิมพ์ข้อความสั้น ๆ เช่น `เพิ่มเกมสังเคราะห์แสง`
5. กด **Commit changes** → รอ 1–2 นาที แล้วเปิดลิงก์ทดสอบ

> ถ้าเบราว์เซอร์ลากโฟลเดอร์ไม่ได้ ให้สร้างโฟลเดอร์ก่อนด้วยวิธีถัดไป แล้วค่อยลากเฉพาะไฟล์เข้าไป

### สร้างโฟลเดอร์ใหม่ (ทริก: พิมพ์ `/` )

1. **Add file ▾ → Create new file**
2. ในช่องชื่อไฟล์ พิมพ์ `photosynthesis/index.html`
   — พอพิมพ์เครื่องหมาย `/` GitHub จะสร้างโฟลเดอร์ให้เอง
3. วางโค้ดเกมทั้งหมดลงในช่องด้านล่าง → **Commit changes**

### แก้ไฟล์เดิม

เปิดไฟล์ที่ต้องการ → กดไอคอน **ดินสอ ✏️** → แก้ → **Commit changes**
(เหมาะกับการแก้ทีละบรรทัด เช่น เปลี่ยนคีย์ Supabase หรือเลขเวอร์ชันแคช)

### ลบ / เปลี่ยนชื่อ

เปิดไฟล์ → ปุ่ม **⋯ → Delete file** หรือกดดินสอแล้วแก้ชื่อไฟล์ในช่องบนสุด
(พิมพ์ `ชื่อโฟลเดอร์ใหม่/ชื่อไฟล์` = ย้ายโฟลเดอร์)

---

## วิธีที่ 2 · GitHub Desktop (แนะนำถ้ามีหลายเกมและอัปบ่อย)

1. โหลด **GitHub Desktop** จาก <https://desktop.github.com> แล้วล็อกอินบัญชี GitHub
2. **File → Clone repository → eatanoi/empty-repository** → เลือกที่เก็บในเครื่อง เช่น `Documents/GitHub/`
3. เปิดโฟลเดอร์นั้นใน File Explorer / Finder แล้ว **ก๊อบปี้โฟลเดอร์เกมใหม่ไปวาง** ได้เลย
4. กลับมาที่ GitHub Desktop จะเห็นรายการไฟล์ที่เปลี่ยน → พิมพ์ข้อความสรุปตรง **Summary** → **Commit to main**
5. กด **Push origin** ด้านบน → เสร็จ

ข้อดี: มีไฟล์ต้นฉบับครบอยู่ในเครื่อง ย้อนเวอร์ชันได้ และอัปไฟล์ใหญ่/เยอะได้สบายกว่าเว็บ

---

## วิธีที่ 3 · คำสั่ง git (สำหรับคนถนัด terminal)

```bash
# ครั้งแรกครั้งเดียว
git clone https://github.com/eatanoi/empty-repository
cd empty-repository

# ทุกครั้งที่จะอัปเกมใหม่
mkdir -p photosynthesis/imag
cp -r ~/Desktop/เกมสังเคราะห์แสง/* photosynthesis/   # ก๊อบไฟล์เกมเข้ามา
git add photosynthesis
git commit -m "เพิ่มเกมสังเคราะห์แสง"
git push
```

---

## ✅ เช็กลิสต์ก่อนอัปเกมใหม่แต่ละเกม

| ข้อ | ทำอะไร | ทำไม |
|---|---|---|
| 1 | ไฟล์หลักชื่อ `index.html` | ลิงก์จะสั้นและจำง่าย |
| 2 | รูปทั้งหมดอยู่ในโฟลเดอร์เดียวกับเกม (เช่น `imag/`) และเรียกแบบ **path สัมพัทธ์** `imag/bg.webp` ไม่ใช่ `/imag/bg.webp` | ขึ้นต้นด้วย `/` จะไปหาไฟล์ผิดที่บน GitHub Pages |
| 3 | เปลี่ยน `og:url` และ `og:image` ให้ตรงลิงก์จริงของเกมนั้น | ตอนแชร์ LINE/Facebook จะขึ้นภาพและชื่อถูกเกม |
| 4 | **คีย์ localStorage ต้องไม่ซ้ำกับเกมอื่น** เช่น `cellular-respiration-quest-v1`, `photosynthesis-quest-v1` | ถ้าซ้ำ ดาวของสองเกมจะทับกัน |
| 5 | ถ้ามีระบบชั้นเรียน ใช้คีย์นำหน้าคนละชุด เช่น `resp-class`, `photo-class` | นักเรียนเข้าคนละห้องในแต่ละเกมได้ |
| 6 | ถ้ามี `sw.js` ให้ตั้งชื่อแคช**ไม่ซ้ำกัน** เช่น `const CACHE = 'photosynthesis-v1'` | แคชคนละเกมจะได้ไม่ลบกันเอง |
| 7 | ใน `manifest.webmanifest` ใช้ `"start_url": "./"` และ `"scope": "./"` | เพิ่มลงหน้าจอโฮมแล้วเปิดเข้าเกมถูกตัว |
| 8 | ใส่เฉพาะคีย์ **anon** ของ Supabase เท่านั้น | คีย์ `service_role` ห้ามขึ้นรีโปสาธารณะเด็ดขาด |

---

## 🌐 ตรวจว่า GitHub Pages เปิดอยู่

1. หน้ารีโป → **Settings → Pages**
2. **Source:** `Deploy from a branch` · **Branch:** `main` · **Folder:** `/ (root)` → **Save**
3. รอสักครู่ จะขึ้นลิงก์ `https://eatanoi.github.io/empty-repository/`

### ถ้าวันหลังอยากผูกโดเมนของตัวเอง

ตอนนี้เว็บอยู่บนที่อยู่ของ GitHub ล้วน ๆ ไม่ได้ผูกโดเมนไหนไว้ ถ้าวันหลังจะผูก
**ลำดับสำคัญมาก** — ทำผิดลำดับแล้วเว็บจะเข้าไม่ได้ทั้งสองที่อยู่

1. **ตั้ง DNS ให้เสร็จและใช้งานได้จริงก่อน** ที่ผู้ให้บริการโดเมน
   - ผูกที่**โดเมนหลัก** (เช่น `example.com`) → เพิ่ม **A record** ที่ Host `@` สี่รายการ
     `185.199.108.153` · `185.199.109.153` · `185.199.110.153` · `185.199.111.153`
   - ผูกที่**ซับโดเมน** (เช่น `games.example.com`) → เพิ่ม **CNAME**: `games` → `eatanoi.github.io`
   - ถ้าชื่อนั้นเคยชี้ไปที่อื่นอยู่ ต้องลบ record เดิมทิ้งก่อน ค่าใหม่ถึงจะมีผล
   - ระวัง **MX record** ถ้าโดเมนนั้นใช้รับอีเมล อย่าลบ

2. **เช็กว่า DNS มาถึงจริงแล้ว** ก่อนไปขั้นต่อไป — ที่ Command Prompt ของวินโดวส์
   พิมพ์ `nslookup ชื่อโดเมน` ต้องขึ้นเลขขึ้นต้น `185.199.` ถ้ายังขึ้น
   *Non-existent domain* แปลว่ายังไม่พร้อม **อย่าเพิ่งทำข้อ 3**

3. **แล้วค่อย** ไป **Settings → Pages → Custom domain** พิมพ์ชื่อโดเมน → **Save**
   (GitHub จะสร้างไฟล์ `CNAME` ในรีโปให้เอง — อย่าลบ)

4. รอจนขึ้นเครื่องหมายถูกสีเขียว แล้วติ๊ก **Enforce HTTPS**

5. แก้ที่อยู่ในโค้ดให้ตรงโดเมนใหม่ — `og:url` `og:image` `twitter:image` ของทุกหน้า
   และข้อความโดเมนที่**วาดอยู่ในภาพ** `og-cover.png` ของแต่ละเกม

**ทำไมลำดับถึงสำคัญ:** พอกรอก Custom domain แล้ว GitHub จะเด้งที่อยู่ `github.io`
ไปหาโดเมนใหม่ทันที ถ้าโดเมนนั้นยังไม่พร้อม จะเข้าไม่ได้ทั้งสองที่อยู่พร้อมกัน

**ถ้าเผลอทำผิดลำดับจนเว็บล่ม** แก้โดย **ลบไฟล์ `CNAME` ออกจากรีโป** แล้วไปล้างช่อง
Custom domain ใน Settings → Pages ให้ว่าง เว็บจะกลับมาที่ `github.io` ตามเดิมภายในไม่กี่นาที

---

## 🏠 หน้ารวมเกม (ทำไปแล้ว)

หน้าแรกของเว็บเป็น **หน้ารวมเกม** และเกมพันธุกรรมย้ายเข้าโฟลเดอร์ `genetics/` เรียบร้อยแล้ว

```
empty-repository/
├── index.html          ← หน้ารวมเกม รายชื่อเกมทั้งหมด
├── genetics/index.html ← เกมพันธุกรรม
├── respiration/index.html
├── cell/index.html
├── … (เกมอื่น ๆ โฟลเดอร์ละเกม)
├── dashboard.html      ← แดชบอร์ดครู
└── check.html
```

เวลาเพิ่มเกมใหม่ ต้องแก้ไฟล์นอกโฟลเดอร์เกม **3 จุด** ไม่อย่างนั้นเกมจะใช้งานไม่ครบ

| ไฟล์ | ต้องเพิ่มอะไร | ถ้าลืมจะเป็นอย่างไร |
|---|---|---|
| `index.html` | การ์ดของเกม + สีประจำเกมในบล็อก `.g-ชื่อเกม` | เข้าถึงได้เฉพาะคนที่รู้ลิงก์ตรง |
| `dashboard.html` | ชื่อเกมในตัวแปร `GAME_NAMES` | แดชบอร์ดครูจะโชว์เป็นชื่อภาษาอังกฤษดิบ ๆ แทนชื่อเกมไทย |
| `sw.js` (นอกสุด) | ชื่อโฟลเดอร์ในรายการที่ให้ข้ามการแคช + ไอคอนใน `ASSETS` + ขึ้นเลข `CACHE` | service worker ของหน้ารวมเกมจะไปแคชไฟล์ของเกมนั้นทับกัน |

> **QR ที่แจกไปแล้ว** — QR เดิมที่ชี้ไปที่ `.../empty-repository/` ตอนนี้จะเปิดเจอ**หน้ารวมเกม**
> แทนที่จะเข้าเกมพันธุกรรมตรง ๆ นักเรียนกดการ์ดใบแรกก็เข้าเกมได้ตามปกติ
> ถ้าอยากให้ QR เข้าเกมตรง ๆ ให้ทำ QR ใหม่ชี้ไปที่ `.../empty-repository/genetics/`

> **ความก้าวหน้าของนักเรียนไม่หาย** — เบราว์เซอร์เก็บ `localStorage` แยกตาม**โดเมน**
> ไม่ได้แยกตาม path ย้ายเกมเข้าโฟลเดอร์แล้วดาวที่เก็บไว้จึงยังอยู่ครบ
> (ทุกเกมในเว็บนี้ใช้โดเมนเดียวกัน จึงตั้งชื่อคีย์ไม่ให้ชนกัน เช่น `genetics_quest_v1` · `metallic-quest-v1`)

---

## 🩹 ปัญหาที่เจอบ่อย

| อาการ | สาเหตุ / วิธีแก้ |
|---|---|
| เปิดลิงก์แล้วขึ้น 404 | ยังไม่ถึง 1–2 นาที · หรือในโฟลเดอร์ไม่มีไฟล์ชื่อ `index.html` · หรือพิมพ์ชื่อโฟลเดอร์ผิดตัวพิมพ์เล็ก-ใหญ่ |
| หน้าเว็บขึ้นแต่รูปไม่ขึ้น | ชื่อโฟลเดอร์/ไฟล์รูปไม่ตรงเป๊ะ (เช่น `imag` กับ `Imag`) หรือเรียกด้วย path ขึ้นต้น `/` |
| แก้เกมแล้วนักเรียนยังเห็นของเก่า | service worker แคชไว้ → แก้เลขใน `sw.js` บรรทัด `const CACHE = '...-v1'` เป็น `-v2` แล้วอัปใหม่ |
| อัปไฟล์ใหญ่ไม่ได้ | หน้าเว็บ GitHub จำกัดไฟล์ละ 25 MB (รีโปควรไม่เกิน ~1 GB) → ย่อรูปเป็น `.webp` หรือใช้ GitHub Desktop |
| อัปแล้วแต่ในรีโปไม่เห็นโฟลเดอร์ย่อยว่าง | git ไม่เก็บโฟลเดอร์เปล่า ต้องมีไฟล์อย่างน้อย 1 ไฟล์ข้างใน |
| เผลออัปคีย์ลับ | ลบไฟล์แล้ว **เปลี่ยนคีย์ใหม่ทันที** ที่ Supabase (ประวัติเก่ายังค้างใน git) |

---

## สรุปขั้นตอนสั้นที่สุด

1. เตรียมโฟลเดอร์เกมในเครื่อง ตั้งชื่ออังกฤษตัวเล็ก ข้างในมี `index.html`
2. เข้ารีโป → **Add file → Upload files** → ลากโฟลเดอร์มาวาง → **Commit changes**
3. รอ 1–2 นาที เปิด `https://eatanoi.github.io/empty-repository/ชื่อโฟลเดอร์/`
4. ทำ QR จากลิงก์นั้นแจกนักเรียน
