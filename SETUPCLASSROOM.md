# 🏫 คู่มือเปิดระบบ "ส่งคะแนนให้ครู"

ระบบนี้ทำให้นักเรียนกรอก **รหัสห้อง + ชื่อ** แล้วคะแนนแต่ละด่านวิ่งเข้า **Google Sheet** ของครูอัตโนมัติ
ใช้เวลาติดตั้งครั้งเดียวประมาณ 15 นาที ฟรีทั้งหมด ไม่ต้องมีเซิร์ฟเวอร์

> เกมยังเล่นได้ครบทุกด่านแม้ไม่เปิดระบบนี้ — ถ้าไม่ตั้งค่า ปุ่ม "เข้าร่วมชั้นเรียน" จะถูกซ่อนไว้เอง

---

## ขั้นที่ 1 · สร้างชีตและสคริปต์

1. สร้าง **Google Sheet** ใหม่ 1 ไฟล์ ตั้งชื่อเช่น `คะแนนเกมพันธุกรรม ม.4`
2. เมนู **Extensions → Apps Script**
3. ลบโค้ดตัวอย่างในนั้นทิ้ง แล้ววางเนื้อหาจากไฟล์ [`apps-script/Code.gs`](apps-script/Code.gs) ลงไปทั้งหมด
4. กด **Save** (ไอคอนแผ่นดิสก์)
5. เลือกฟังก์ชัน `setup` จากเมนูดรอปดาวน์ด้านบน แล้วกด **Run**
   - ครั้งแรกจะขึ้นหน้าขออนุญาต → **Review permissions** → เลือกบัญชีของคุณ
   - ถ้าขึ้นคำเตือน "Google hasn't verified this app" → กด **Advanced** → **Go to … (unsafe)** → **Allow**
     (ปลอดภัย เพราะเป็นสคริปต์ที่คุณเขียนเองในบัญชีตัวเอง)
   - เสร็จแล้วกลับไปดูที่ชีต จะมีแท็บชื่อ `scores` พร้อมหัวตารางขึ้นมาให้

## ขั้นที่ 2 · Deploy เป็น Web app

1. ปุ่ม **Deploy → New deployment**
2. ไอคอนเฟือง ⚙️ ข้างคำว่า *Select type* → เลือก **Web app**
3. ตั้งค่า:
   - **Execute as:** `Me (อีเมลของคุณ)`
   - **Who has access:** `Anyone`  ← สำคัญมาก ถ้าเลือก "Anyone with Google account" นักเรียนจะส่งไม่ได้
4. กด **Deploy** แล้วคัดลอก **Web app URL** (ลงท้ายด้วย `/exec`)
5. ทดสอบ: เปิด URL นั้นในเบราว์เซอร์ ต้องเห็นข้อความ
   ```json
   {"ok":true,"service":"genetics-quest","message":"พร้อมรับคะแนนแล้ว"}
   ```

## ขั้นที่ 3 · ใส่ URL ลงในเกม

1. เปิดไฟล์ `index.html` ด้วยโปรแกรมแก้ไขข้อความ (Notepad, VS Code, TextEdit ก็ได้)
2. กด `Ctrl+F` ค้นคำว่า `CLASS_ENDPOINT` จะเจอบรรทัดนี้ (อยู่ราวบรรทัดที่ 320)

   ```js
   const CLASS_ENDPOINT = '';
   ```

3. วาง URL ลงในเครื่องหมายคำพูด

   ```js
   const CLASS_ENDPOINT = 'https://script.google.com/macros/s/AKfycb...../exec';
   ```

4. บันทึกไฟล์ แล้วอัปโหลดทับขึ้น GitHub
   (หน้า repo → ➕ → **Upload files** → ลากไฟล์ใหม่มาวาง → **Commit changes**)
   GitHub Pages จะอัปเดตให้เองภายใน 1–2 นาที **QR เดิมใช้ต่อได้ ไม่ต้องพิมพ์ใหม่**

## ขั้นที่ 4 · ใช้งานในห้องเรียน

1. ครูกำหนดรหัสห้องเอง เช่น `M401A`, `M402B` (ตัวอักษร+ตัวเลข ไม่ต้องลงทะเบียนที่ไหน)
2. เขียนรหัสห้องไว้บนกระดานพร้อม QR
3. นักเรียนสแกน → หน้าหลักจะมีปุ่ม **🏫 เข้าร่วมชั้นเรียน** → กรอกรหัสห้อง + ชื่อ–เลขที่ → เล่นได้เลย
4. จบแต่ละด่าน คะแนนจะเข้าชีตทันที (ถ้าเน็ตหลุด จะพักไว้แล้วส่งเองเมื่อกลับมาออนไลน์)

---

## 📊 สูตรทำหน้าสรุปให้ครูดูง่าย

สร้างแท็บใหม่ชื่อ `สรุป` แล้ววางสูตรเหล่านี้

**ช่อง B1** ใส่รหัสห้องที่ต้องการดู เช่น `M401A`

**สรุปรายคน** (ดาวรวมสูงสุด จำนวนด่านที่จบ เวลารวม)

```
=QUERY(scores!A:O, "select C, max(M), count(A), sum(K) where B = '"&B1&"' group by C order by max(M) desc label max(M) 'ดาวรวม', count(A) 'จำนวนครั้งที่จบด่าน', sum(K) 'เวลารวม(วินาที)'", 1)
```

**ดาวที่ดีที่สุดของแต่ละคน แยกตามด่าน** (ตารางไขว้ เห็นทันทีว่าใครติดด่านไหน)

```
=QUERY(scores!A:O, "select C, max(G) where B = '"&B1&"' group by C pivot E", 1)
```

**ด่านที่ทั้งห้องพลาดเยอะที่สุด** (ใช้เลือกว่าควรทบทวนเรื่องไหน)

```
=QUERY(scores!A:O, "select F, avg(H), avg(L) where B = '"&B1&"' group by F order by avg(H) asc label avg(H) 'คะแนนเฉลี่ย %', avg(L) 'ข้อที่พลาดเฉลี่ย'", 1)
```

---

## 🔧 แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุ / วิธีแก้ |
|---|---|
| เปิด URL แล้วขึ้น "Script function not found: doGet" | ยังไม่ได้ Deploy ใหม่หลังวางโค้ด → Deploy → **Manage deployments** → ✏️ → Version: **New version** → Deploy |
| นักเรียนเล่นแล้วไม่มีข้อมูลเข้าชีต | ตรวจว่า *Who has access* = **Anyone** และ URL ที่วางลงท้ายด้วย `/exec` (ไม่ใช่ `/dev`) |
| ในเกมขึ้น "รอส่ง" ค้างไม่หาย | เปิดหน้า **📤 สถานะการส่งคะแนน** ในเกม จะบอกสาเหตุ แล้วกด **ส่งเดี๋ยวนี้** |
| Console เบราว์เซอร์ฟ้อง CORS | แก้ `const NO_CORS_MODE = false;` เป็น `true` ในไฟล์ `index.html` แล้วอัปโหลดใหม่ (โหมดยิงแล้วไม่รอผลตอบกลับ) |
| อยากจำกัดเฉพาะห้องที่กำหนด | ในไฟล์ `Code.gs` แก้ `CHECK_ROOM = false` เป็น `true` แล้วใส่รหัสห้องในแท็บ `rooms` คอลัมน์ A จากนั้น Deploy เวอร์ชันใหม่ |

---

## ⚖️ ข้อควรรู้ก่อนใช้เก็บคะแนนจริง

- **กันโกงไม่ได้ 100%** ทุกอย่างรันในเครื่องนักเรียน คนที่รู้เทคนิคสามารถแก้ค่าใน localStorage หรือยิงข้อมูลปลอมได้
  → เหมาะกับ **คะแนนการมีส่วนร่วม / การบ้านฝึกฝน** มากกว่าคะแนนสอบ
  → ชีตบันทึก *เวลาที่ใช้* และ *ครั้งที่เล่น* ไว้ให้แล้ว ถ้าใครได้ 100% ใน 20 วินาที จะเห็นผิดปกติทันที
- **PDPA** ระบบเก็บเฉพาะรหัสห้อง ชื่อที่นักเรียนกรอกเอง และผลการเล่น ไม่มีอีเมล เบอร์โทร หรือตำแหน่งที่ตั้ง
  แนะนำให้ตั้งชีตเป็นส่วนตัว (อย่าแชร์แบบ Anyone with the link ที่แก้ไขได้) แจ้งนักเรียนว่าเก็บไปทำอะไร และลบข้อมูลเมื่อจบภาคเรียน
- **นักเรียนออกจากระบบได้เอง** ผ่านปุ่ม 🚪 ออกจากชั้นเรียน ซึ่งจะหยุดส่งข้อมูลทันที
